# Alembic migrations

This directory is the migration home for the new SQLAlchemy/Alembic backend
structure. The current app still uses `local_db.py` as a compatibility
repository while tables are moved to ORM models incrementally.

